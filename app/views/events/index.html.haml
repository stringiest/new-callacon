%h1.text-center Callacon events
%table.table.table-striped
  %thead
    %tr
      %th{ scope: 'col' } Name
      %th{ scope: 'col' } Actions
  %tbody
    - @events.reverse.each do |event|
      - next unless current_user

      %tr
        %td= event.name
        %td
          = link_to 'Edit', edit_event_path(event), class: 'btn btn-primary'
          = button_to 'Delete', event_path(event), method: :delete, data: { confirm: 'Are you sure you want to delete this whole event?' }, class: 'btn btn-danger ml-4'